# =============================================================================
# Datasource PostgreSQL compartilhado com o serviço de submissão de feedbacks
# As variaveis de ambiente substituem os valores padrão em produção
# =============================================================================
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${DB_USERNAME:postgres}
quarkus.datasource.password=${DB_PASSWORD:postgres}
quarkus.datasource.jdbc.url=${DB_URL:jdbc:postgresql://localhost:5432/postgres}

# Hibernate não gerencia o schema, o Flyway cuida das migrations
quarkus.hibernate-orm.database.generation=none

# =============================================================================
# Flyway migrations automaticas em produção
# =============================================================================
quarkus.flyway.migrate-at-start=true
quarkus.flyway.locations=classpath:migration

# =============================================================================
# E-mail (SMTP) configurar via variaveis de ambiente em produção
# Para Gmail use smtp.gmail.com:587 com uma App Password
# =============================================================================
quarkus.mailer.host=${MAIL_HOST:smtp.office365.com}
quarkus.mailer.port=${MAIL_PORT:587}
quarkus.mailer.start-tls=REQUIRED
quarkus.mailer.username=${MAIL_USERNAME:rm367471@fiap.com.br}
quarkus.mailer.password=${MAIL_PASSWORD:rfWY7R7VL63cL7J}
quarkus.mailer.from=${MAIL_FROM:rm367471@fiap.com.br}

# Destinatário padrão do relatório semanal
relatorio.email.destinatario=${RELATORIO_EMAIL_DESTINATARIO:rm367471@fiap.com.br}

# =============================================================================
# Azure Functions app configurações para deploy via azure-functions-maven-plugin
# =============================================================================
quarkus.azure-functions.app-name=${azure.function.app.name:avaliacoes-1778625830017}
quarkus.azure-functions.runtime.java-version=21

${azure.function.app.name:tech-challenge-relatorio}

# =============================================================================
# OpenAPI / Swagger UI
# =============================================================================
quarkus.swagger-ui.always-include=true
mp.openapi.extensions.smallrye.info.title=Tech Challenge Relatório API
mp.openapi.extensions.smallrye.info.version=1.0.0
mp.openapi.extensions.smallrye.info.description=Serviço serverless de geração de relatórios semanais de feedbacks

# =============================================================================
# Perfil de desenvolvimento PostgreSQL do docker-compose (pg-dev)
# =============================================================================
%dev.quarkus.datasource.db-kind=postgresql
%dev.quarkus.datasource.username=postgres
%dev.quarkus.datasource.password=postgres
%dev.quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/postgres
%dev.quarkus.hibernate-orm.database.generation=none
%dev.quarkus.flyway.enabled=true
%dev.quarkus.flyway.migrate-at-start=true
%dev.quarkus.flyway.repair-at-start=true
%dev.quarkus.mailer.mock=true

# =============================================================================
# Perfil de testes
# =============================================================================
%test.quarkus.datasource.db-kind=h2
%test.quarkus.datasource.jdbc.url=jdbc:h2:mem:feedback_test;DB_CLOSE_DELAY=-1
%test.quarkus.hibernate-orm.database.generation=drop-and-create
%test.quarkus.hibernate-orm.sql-load-script=import.sql
%test.quarkus.flyway.enabled=false
%test.quarkus.mailer.mock=true